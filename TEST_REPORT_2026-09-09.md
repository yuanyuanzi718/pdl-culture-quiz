# 上线前测试报告

2026年9月9日完成。本地可以进入用户复测阶段，尚未部署云端。生产基础设施、公网HTTPS和Linux环境仍需部署时验收。

## 验证结果

| 项目 | 结果 |
|---|---|
| 后端自动化测试 | 30项全部通过，包含原10项及新增20项 |
| 后端类型检查 | 通过 |
| 后端正式构建 | 通过，生成可直接用Node运行的dist/index.js |
| 前端正式构建 | 通过，已适配/pdltk/ |
| 前后端生产依赖审计 | 已知漏洞均为0，仅指本次生产依赖审计结果 |
| 273道题库 | 数据库与JSON逐条一致，答案格式及题目去重检查通过 |
| SQLite完整性及外键 | integrity_check为ok，外键错误0 |
| 浏览器试卷闭环 | 实际完成10题，得40分，答对4题，结果与个人历史一致 |
| 刷新和切页续答 | 保留试卷、答案和进度，不重复扣题数 |
| 真实AI调用 | 共4次实际请求成功，覆盖资料引用、连续追问和第3次免费额度边界 |
| AI历史与安全 | 新回答来源刷新后保留；HTML事件属性和危险链接过滤通过 |
| 管理员发码闭环 | 在独立库通过UI登记、发码、激活、解绑，未进行实际转账或对外发消息 |
| 身份恢复 | 失效令牌自动恢复同设备身份通过 |
| 旧支付和短信入口 | 原本地服务实测全部404，不再产生假成功 |
| 生产构建浏览器 | /pdltk/路由、API代理、问答正常，无正常路径控制台错误 |
| 生产密钥门禁 | 缺少强密钥时启动失败，不带默认凭证上线 |
| 发布包 | 25个文件哈希核验通过，不含数据库、密钥、node_modules或测试代码 |

## 修复内容

- 移除支付宝假下单、无验签支付回调、短信假发送和前端旧调用入口。
- 增加管理员人工收款登记，要求真实交易单号和实际到账确认；重复交易不会重复发码，不可改绑其他买家。
- 激活码绑定实际用户与设备，限制重复替换、跨用户领取；到期按发放时间计算，解绑后可重新迁移。
- 抽题不返回答案解析；领取时保存试卷快照，防止篡改题号、绕过额度、重复交卷以及管理端编辑影响已发试卷。
- 免费额度不足10题时只发剩余额度；刷新或重新进入恢复同一试卷。
- 修复多选答案在结果页显示JSON字符串的问题。
- AI配置缺失、空回答或上游失败时明确报错，不生成替代答案、不扣对话额度；增加超时、并发保护和历史上下文。
- 修复历史加载覆盖新问题、最后一次免费回答被遮罩挡住、刷新丢失新回答来源等问题。
- 使用DOMPurify过滤AI生成的HTML，升级路由库解决生产依赖审计中的两项中危公告。
- 移除源码和示例文件中的内置密钥，更新本地随机鉴权密钥；增加请求限流、体积限制和题库输入校验。
- 匿名用户不再生成虚构手机号，后台按用户编号展示。
- 修复部署文档与实际构建命令、子路径、入口文件不一致；构建前清理旧dist，避免残留已删除的假支付代码。

## 本地数据清理

保留273道题、48个原有用户、27次考试、58条对话。删除7笔旧假支付订单、7个关联激活码，撤销对应6个VIP状态，并清除另外4个没有有效激活码的旧VIP状态。清理后订单、激活码、有效VIP均为0。

测试过程中曾发现数据库路径在模块加载时固化，第一轮新增测试误写本地库。已停止该轮测试，依据题库JSON和测试前记录边界恢复原有数据、题号及记录，逐项核对数量与完整性。之后将路径改为连接时读取，并禁止测试环境使用业务数据库；后续完整回归未再改变业务数据。恢复前状态及清理前一致性备份位于 `output/qa/private-backups/`，仅供本地恢复，不进入发布包。

之后的集成测试使用独立临时SQLite，浏览器使用独立数据库。测试代码中的HTTP样例响应只用于验证失败与并发分支，不是产品运行时的降级逻辑。所有临时浏览器账户、验收交易和安全测试文本均不进入业务库或发布包。

## 发布材料与边界

- 人工复测：`MANUAL_TEST.md`。
- 生产部署步骤：`DEPLOY_CHECKLIST.md`。
- 已核验发布目录：`output/release-2026-09-09T13-35-34-479Z/`，清单为 `manifest.json`。
- 首次上线用题库JSON初始化新数据库，不上传当前本地业务库。
- 当前付款方式是微信人工核实收款，不是支付平台自动验签到账。客服信息与9.9元价格应由用户复测时确认。
- 本轮未验证真实资金收付、云上域名备案/证书、反向代理最终配置、Linux原生依赖安装或公网负载。这些留到用户验收后的部署阶段。
- 原有233题的逐题内容事实审校不属于本轮程序测试；本轮完成结构、答案格式和系统读取的一致性验证。

管理员令牌存放于 `server/.env` 的 `ADMIN_TOKEN`，旧默认令牌不再有效。报告和发布包均不包含明文凭证。

## 后台移动端适配追加验证

2026年9月9日22时完成：移动端底部五栏导航、固定顶部工具栏、列表卡片化、全屏题目表单、触控尺寸和输入字号调整。桌面保留侧栏与表格。

浏览器在320、390、430、768、1440像素宽度下遍历后台五个页面，共25组检查，无横向溢出。题目表单在320、390、430宽度及568高度下验证全屏边界和保存按钮可见性，空题校验、判断选项自动设置、取消关闭通过。手机输入及安全区域使用响应式样式支持，尚待用户真机验证软键盘行为。本轮未新增或修改业务题目、订单数据。

前端构建通过。最新发布目录为 `output/release-2026-09-09T14-32-45-909Z/`，替代上述旧前端发布包；尚未部署云端。
