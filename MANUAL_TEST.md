# 上线前复测清单

更新于 2026年9月9日。本轮自动化与浏览器测试已经执行；请按以下步骤进行人工验收。当前没有部署云端。

## 本地入口

- 用户端：http://localhost:5173/
- 管理端：http://localhost:5173/admin
- 管理员令牌读取 `server/.env` 中的 `ADMIN_TOKEN`。旧默认令牌已停用，报告不记录明文凭证。
- 如页面保留了旧版本，请刷新。旧登录令牌失效后应自动恢复同设备身份。

## 用户端

1. 打开评估页，点击开始考试。正常题库下每套10题，4单选、4判断、2多选；剩余额度不足10题时只发放剩余题数。
2. 单选选1项，多选至少选2项。未完成当前题时下一题不可用。
3. 答到第2题后刷新：原试卷、已选答案和当前位置应保留，不重复消耗额度。
4. 点击“这道题不会？问 AI”：自动发送题目，显示真实回答；问题不应被历史加载覆盖。返回评估页继续原试卷。
5. 完成交卷：检查分数、答对题数、错题答案及解析。多选答案显示为 A、B 等字母，不显示 JSON 字符串。
6. 打开“我的”：成绩与本次交卷一致，抽题扣一次额度，交卷不重复扣。点击“再来一次”，额度用尽应提示解锁。
7. 在对话页输入文化问题，再追问“刚才说的是什么意思”：回答应承接上下文。刷新后历史和新回答的来源仍可查看。
8. 断开网络或后端服务后操作：显示错误，可重试；不得出现假回答、假成绩或假成功。
9. 检查“立即解锁”弹窗：当前流程是联系微信客服，页面不会直接模拟付款成功。

## 管理端

1. 输入错误令牌，应拒绝登录；正确令牌应进入数据概览。
2. 题库总数应为273；本轮清理后旧假订单及激活码均为0。原有访问、考试、对话历史仍保留，后续真实访问会自然增加统计。
3. 题库管理：搜索“图片文字整理”检查新增题。可编辑、保存一题后再恢复原文；不要把验收题留在正式题库。
4. 用户管理显示用户编号，不再把自动生成的游客占位字符串显示成手机号。
5. VIP发放采用人工核对到账：填写用户“我的”页面中的用户编号、真实微信交易单号，确认实际到账后登记。相同交易单号重试不会重复发码；不能登记给另一个用户。不要为了验收编造实际收款。
6. 用户收到对应激活码后激活，VIP可继续刷题。激活码绑定当前用户和设备；换设备前先解绑。
7. 初次激活按发放时间计算有效期；解绑后重新获得有效期，可转移至另一设备。

## 已完成的隔离验证

真实SQLite临时数据库中完成了题库增删改查、全对满分、少选/多选判分、越权拒绝、伪造试卷拒绝、重复交卷、额度边界、发码幂等、激活解绑迁移、过期、AI异常不扣费及限流测试。浏览器也执行了真实AI调用和VIP闭环，没有实际发起收款或向他人发送消息。

## 验收结论

- [ ] 评估、刷新续答、成绩和额度正常
- [ ] AI问答、连续追问、历史和错误反馈正常
- [ ] 管理端题库、用户、发码流程符合预期
- [ ] 微信客服信息和价格确认正确
- [ ] 确认生产域名、HTTPS及服务器部署位置

勾选完成后再进行云端部署和公网验收。

## 手机后台复测

电脑与手机连接同一Wi-Fi时，打开 `http://192.168.1.133:5173/admin`（本机当前局域网地址，网络变化后需重新确认）。检查底部五栏切换、题库搜索与翻页、编辑表单滚动与取消、VIP发码表单输入。输入时确认手机软键盘不挡住操作，底部导航避开系统手势区。此次浏览器已检查320至1440像素宽度；实际手机软键盘仍需真机确认。
